{% extends 'base.html.twig' %}

{% block title %}TaskPost index{% endblock %}

{% block body %}
    <h1>TaskPost index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Title</th>
                <th>Status</th>
                <th>Posted</th>
                <th>PostText</th>
                <th>MediaUrls</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for post in schedule_posts %}
            <tr>
                <td>{{ post.id }}</td>
                <td>{{ post.name }}</td>
                <td>{{ post.title }}</td>
                <td>{{ post.status ? 'Yes' : 'No' }}</td>
                <td>{{ post.posted ? 'Yes' : 'No' }}</td>
                <td>{{ post.postText }}</td>
                <td>{{ post.mediaUrls ? post.mediaUrls|json_encode : '' }}</td>
                <td>
                    <a href="{{ path('app_task_post_show', {'id': post.id}) }}">show</a>
                    <a href="{{ path('app_task_post_edit', {'id': post.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_task_post_new') }}">Create new</a>
{% endblock %}
