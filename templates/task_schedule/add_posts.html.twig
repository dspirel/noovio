{% extends 'base.html.twig' %}

{% block title %}Select Task Posts{% endblock %}

{% block body %}
    <h1>Select Task Posts</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Submit</button>
    {{ form_end(form) }}

    <a href="{{ path('app_task_schedule_index') }}">back to list</a>
{% endblock %}

